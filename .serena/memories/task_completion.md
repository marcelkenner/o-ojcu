# Task Completion Checklist
- Run `source ~/.nvm/nvm.sh && npm run build` (ensures tsc + static copy succeed).
- Verify dist output contains pages, styles, js; open `dist/index.html` with static server.
- Ensure new/updated processes are reflected in `docs/website_repro_playbook.md`.
- Keep files under 300 lines; split if approaching limit.
